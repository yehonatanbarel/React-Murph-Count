import React from "react";
import './style.css'

function StopWatch() {

    const [time, setTime] = React.useState({ ms: 0, s:0, m:0, h:0});

    return (
        <div>

        </div>
    )
}

export default StopWatch;